Quick update on today’s deployment activities.

We were able to successfully deploy IDH, IMRR, and API. I reviewed the application logs after deployment, and everything is working as expected.

For Disaster Recovery (DR), I manually updated the traffic in GCP to point to the newer Cloud Run revisions/images and verified that the DR services are now working as expected. The DR traffic update was the part that was causing the pipeline issue this morning. I have updated the pipeline configuration/code to handle the DR traffic update automatically, and those changes are now in the repository.

Additionally, I successfully completed the one-time Firestore scan/update job. The job added the three additional fields to the documents in the Firestore Employee collection as planned.

At this point, the deployments, DR configuration, and Firestore update have all been completed successfully.